# Copy this file to `.env` if you need to override the default.
#
# There is NO API key here any more. The Gemini key lives in backend/.env and
# never reaches the browser - the frontend only needs to know where Laravel is.

VITE_API_BASE_URL=http://localhost:8000
